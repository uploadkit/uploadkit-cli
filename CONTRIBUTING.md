# Contributing

```bash
pip install -e ../uploadkit -e ../uploadkit-security -e ../uploadkit-testing
pip install -e ../uploadkit-pdf -e ../uploadkit-office -e ../uploadkit-audio
pip install -e ".[dev]"
pytest
```

Keep this package free of framework adapters.
